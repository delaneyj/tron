# Suggested commands

Run from `implementations/go/vnt`:

- Tests: `go test ./...`
- Benchmarks (per README): `go test -bench . -benchmem -run=^$ ./...`
- Format: `gofmt -w .`

Repo root utilities (Linux): `rg --files`, `rg -n <pattern> <path>`, `ls -la`, `cat <file>`.
